# GENPANO 全项目一致性审查报告 — 2026-04-21

**审查范围**: App 侧 + Admin 侧 · PRD / SESSIONS / Harness / DESIGN_TOKENS / frontend
**审查维度** (用户四问):
1. 功能一致: Session 给 Claude Code 能跑出前后端通的系统
2. Data Pipeline 可实现且可细粒度监控
3. 边界异常覆盖完整
4. 测试场景齐全

**审查方式**: 4 个并行 Explore agent 分别审 4 维度 + 主线程 spot-verify P0 发现 (5 处抽查)
**整体结论**: 文档成熟度 **75%** (PRD 内容密度极高、决策链完整) · 实施成熟度 **25%** (App frontend 约 60% 迁入 V2 IA, Admin frontend 完全未起) · 测试成熟度 **5%** (策略纸面完整, 代码零条测试)

---

## 0. Executive Summary — 8 个必修 P0 (阻塞)

| # | 类别 | 问题 | 所在层 |
|---|------|------|--------|
| P0-1 | 测试基础设施 | 零 test runner: 无 vitest.config / playwright.config / `frontend/tests/` / `scripts/ci-check.mjs`, `package.json` 只 dev/build/preview | 测试 |
| P0-2 | Harness 流于纸面 | CLAUDE.md ~30 条 grep / C1-C7 / C9-C15 无 pre-commit / 无 CI workflow, 全部"aspirational" | 测试 |
| P0-3 | V2 IA 实施缺口 | brand/ 缺 4 页 (Overview / Topics / Diagnostics / Reports), industry/ 缺 1 页 (KnowledgeGraph), 13+ legacy 页未 301 / 未删 | App frontend |
| P0-4 | 布局组件未建 | ModeToggle / BrandPicker / IndustryPicker 全缺, 新 IA 无法渲染 | App frontend |
| P0-5 | Intent Matrix 未规格化 | `Topic.dimension × Topic.industry → Intent[]` 决策表不存在, Prompt 生成不可确定性实现 | Pipeline |
| P0-6 | 成本/延迟可观测性缺字段 | `ai_responses` 无 `cost_usd/cost_cny/token_count` 与延迟分段字段, Admin 无法回答"哪个引擎本周最贵" | Pipeline 监控 |
| P0-7 | Citation Tier CRUD + MCP Token 签发未分配 Session | 决策 #19 要求 Tier 不硬编码 + App MCP token Day1, A0-A4 均无此任务, App Session 3 MCP 集成会失败 | Admin |
| P0-8 | KG Mined Entity Schema 缺失 | 促进规则 (confidence ≥ 0.85 → canonical) / `kg_mined_relations` 表 / mining job 均未建模 | Pipeline |

---

## 1. 维度一 · 功能一致性

### 1.1 App 侧: PRD ↔ SESSIONS ↔ frontend

**PRD 内部一致性**: ✅ 超出预期

Spot-verify 结果 (主线程 grep PRD.md):

| §章节 | SUPERSEDED 标记位置 | 状态 |
|---|---|---|
| §4.1.1c | PRD line 732 | ✅ 已标 (by §4.1.1-gate) |
| §4.1.1d | PRD line 1013 | ✅ 已标 (by §4.6-IA-v2.F) |
| §4.6.1 | PRD line 3921 | ✅ 已标 (by §4.6-IA-v2) |
| §4.6.1-0 | PRD line 3970 | ✅ 已标 (同日废弃) |
| §4.6.1a | PRD line 4070 | ✅ 已标 (渲染锚点迁移 `/brand/overview`) |
| §4.6.1b | PRD line 4393 | ✅ 已标 (4 Tab 展平为 9 sub-view) |
| §4.6.1d | PRD line 4505 | ✅ 已标 (brandId 走 query string, §O Wave-4) |
| §4.6.1e | PRD line 4543 | ✅ 已标 (`/industry/overview`) |

PRD 共出现 SUPERSEDED/SUPERSEDES 16 处, 迁移踪迹完整. 维度一审查 agent 原报告所称"§4.6.1 系列无废弃标"**不准确**, 本报告已更正.

**frontend ↔ V2 IA 实施缺口** (verified `ls frontend/src/pages/brand/ frontend/src/pages/industry/`):

期望 (CLAUDE.md 第 2 条决策 + §设计锚点 line 120-146) vs 实际:

| 期望路径 | 当前文件 | 状态 |
|---|---|---|
| `/brand/overview` | `pages/brand/BrandOverviewPage.jsx` | ❌ **缺** (DashboardPage.jsx 仍在 top-level, 未 301) |
| `/brand/visibility` | `pages/brand/BrandVisibilityPage.jsx` | ✅ 存在 |
| `/brand/topics` | `pages/brand/BrandTopicsPage.jsx` | ❌ **缺** (top-level TopicsPage.jsx 未改造/未 301) |
| `/brand/sentiment` | `pages/brand/BrandSentimentPage.jsx` | ✅ 存在 |
| `/brand/citations` | `pages/brand/BrandCitationsPage.jsx` | ✅ 存在 |
| `/brand/products` | `pages/brand/BrandProductsPage.jsx` | ✅ 存在 |
| `/brand/products/:productId?brandId=` | `pages/brand/BrandProductDetailPage.jsx` | ✅ 存在 (Wave-4 契约 C15-1/2/3 须核实) |
| `/brand/competitors` | `pages/brand/BrandCompetitorsPage.jsx` | ✅ 存在 |
| `/brand/diagnostics` | `pages/brand/BrandDiagnosticsPage.jsx` | ❌ **缺** |
| `/brand/reports` | `pages/brand/BrandReportsPage.jsx` | ❌ **缺** (top-level ReportsPage.jsx 未改造) |
| `/industry/overview` | `pages/industry/IndustryOverviewPage.jsx` | ✅ 存在 |
| `/industry/ranking` | `pages/industry/IndustryRankingPage.jsx` | ✅ 存在 |
| `/industry/topics` | `pages/industry/IndustryTopicsPage.jsx` | ✅ 存在 (v3.2 对齐度待核) |
| `/industry/knowledge-graph` | `pages/industry/IndustryKnowledgeGraphPage.jsx` | ❌ **缺** (top-level KnowledgeGraphPage.jsx 未 301) |

**缺页合计**: 5 个 (brand/4 + industry/1)

**legacy top-level 页未清理** (13 个, CLAUDE.md §2 设计锚点已标"废弃"但仍在代码里):
DashboardPage.jsx / DashboardPage.linear.jsx / BrandDetailPage.jsx / DiagnosticsPage.jsx / IndustryPage.jsx / KnowledgeGraphPage.jsx / ReportsPage.jsx / TopicsPage.jsx / ProductsPage.jsx / LandingPageLegacy.jsx / BrandSimulatorPage.jsx / QueriesPage.jsx / BrandsPage.jsx (最后一个保留为"品牌集市 grid", 其它应删或 301)

**缺布局/侧栏组件** (CLAUDE.md 第 2 条决策 + 设计锚点 line 120-123):
- `components/topbar/ModeToggle.jsx` (T1') — Stripe pill toggle 🎯 品牌 ⇌ 🌍 行业
- `components/sidebar/BrandPicker.jsx` (T2')
- `components/sidebar/IndustryPicker.jsx` (T3')
- `layouts/DashboardLayout.jsx` 需重写为 Mode-aware 侧栏

**→ 结论**: T1' / T2' / T3' Session 全部未执行或未完整执行. 现状下 `/brand/*` 与 `/industry/*` 路由无法渲染.

### 1.2 App 侧: i18n 架构与 PRD §4.10.4a 不匹配

Spot-verify `ls frontend/src/i18n/`:
```
messages.js
messages.js.broken-backup    ← 注意: 最近有一次破坏性改动
```

- 单文件 `messages.js` (无 namespace 拆分)
- **无** `alert / settings / brand / product / report / diagnostics / common` 子命名空间
- **无** `formatBrand` utility 包装 nameZh/nameEn 逻辑 (§4.10.4a.D)
- `.broken-backup` 说明最近曾试图修改但失败
- Prompt 多语言字段 (engine-aware) 暂无前端消费 (合理: 该字段属 Pipeline 后端, frontend 仅展示)

### 1.3 Admin 侧: ADMIN_PRD ↔ ADMIN_SESSIONS ↔ admin frontend

Spot-verify: **无 admin frontend 代码** (`ls admin*/ admin-frontend*/ frontend-admin*/` 全空). 只有 `design/prototype-admin.html` 单文件 HTML 原型.

**PRD ↔ SESSIONS 模块覆盖矩阵** (3 份 PRD × 1 份 SESSIONS):

| 模块 | PRD 章节 | Session 任务 | 状态 |
|------|----------|--------------|------|
| A (Accounts) | ADMIN_PRD §4.1 | A1 §7 | ✅ 全覆盖 |
| B (Pipeline Overview/Planner/Queue/AccountPool/ProxyPool) | ADMIN_PRD_B §1-2 | A2 §2-5 | ✅ 全覆盖 |
| B (Failure Retry + HAR Replay) | ADMIN_PRD_B §4.2.6 + ADAPTER_CONTRACT §10.2 | A2.2 | ⚠️ **部分**: PRD 规定"admin sandbox 回放 `page.routeFromHAR()`", Session 只列"下载 HAR" |
| B (Prompt Templates + ProfileGroup) | ADMIN_PRD_B §1.3-1.4 | A2.1 | ✅ 全覆盖 |
| B (Analyzer + QA 抽样) | ADMIN_PRD_B §3.1-3.2 | A2.4 | ⚠️ **部分**: QA 每日自动抽样 200 条分层算法未在 Prompt 里展开 |
| B (Change Requests + Trace) | ADMIN_PRD_B §2.4 / §4.3 | A2.3 | ✅ 覆盖, 但 change_type enum 缺 KG-specific 值 (见下) |
| C (KG Industries/Categories) | ADMIN_PRD_C §C1 | A3 | ✅ |
| C (Brand / Product Audit) | ADMIN_PRD_C §C2-C3 | A3 | ✅ |
| C (Brand Submissions + Trust Score) | ADMIN_PRD_C §C5 (trust_score 4 档) | A3 | ❌ **Trust Score 系统缺失 Session 任务** |
| C (Discovery Logs + Hallucination) | ADMIN_PRD_C §C6 (secondary LLM verify) | A3 | ❌ **Hallucination 检测缺失 Session 任务** |
| C (Entity Merge/Split) | ADMIN_PRD_C §C7 | A3.1 | ✅ (但 A3.1 是 A3 后的扩展, 若 A3 超时易延期) |
| C (KG Daily Snapshot Diff) | ADMIN_PRD_C §C8 | A3.2 | ⚠️ 表/Job/Cron/checksum 算法未细化 |
| C (KG Quality Monitor) | ADMIN_PRD_C §C9 | A3.3 | ✅ |
| D (Cost/Alerts/Commercial/Audit) | ADMIN_PRD §4.4 | A4 | ⚠️ 本次未抽查 A4 Prompt, 外审继续 |
| **新: Citation Tier CRUD + MCP Token** | CLAUDE.md 决策 #19 | **未分配** | ❌ **P0 阻塞 App MCP 集成** |

**Admin 最大风险**: Session A3 的 scope 未覆盖 ADMIN_PRD_C §C5/§C6 (Trust Score + Hallucination 检测), 且决策 #19 的 Citation Tier CRUD + MCP Token 签发没有任何 Session 承接. 这会导致 App Session 3 开始 MCP 集成时发现无 token 可签发、Tier 权重只能临时硬编码 (违反决策 #19).

---

## 2. 维度二 · Data Pipeline 实现性 + 细粒度监控

### 2.1 四层 Pipeline 实现力评估

| 层 | 实现力 | 关键缺口 |
|----|--------|----------|
| Topic (Planner) | 70% | **Intent Matrix 决策表缺失** (P0-5): `Topic.dimension × Topic.industry → Intent[]` 未规格化; LLM 初始化品类树的具体 system prompt 未入库 |
| Topic → Prompt | 60% | Intent 分配规则无确定性; §4.10.3 说"记录 `language` + `appliesToEngines[]`" 但谁生成、何时剔重、版本化粒度均未细化 |
| Prompt → Query | 85% | Profile 抽样 `sampleProfile(profileGroupIds, seed?)` 已规 (ADAPTER §3.4), ProfileGroup 内概率分布未指定 (均匀? 加权?) |
| Query → Response | 90% | ADAPTER_CONTRACT §6-10 高完成度; 仅个别异常分支 (0 健康代理节点 / 帐号预热) 欠清 |
| Response → KG Mining | 35% | **严重缺口**: `kg_mined_relations` 表不存在; 置信度累积规则 (0.6→+0.1→0.9) 在 PRD §4.0.1a Phase 2 只有文字描述, 无 schema / job / promotion trigger |

### 2.2 Engine-aware Prompt 语言正确性

- 字段存在: ✅ `prompt_templates.applies_to_engines TEXT[]` (ADMIN_PRD_B §1.3), ExecutableQuery.language (ADAPTER §2.1)
- 规则清晰: ✅ 豆包/DeepSeek=zh-CN only, ChatGPT=zh+en bilingual (PRD §4.10.3)
- 生成逻辑: ⚠️ **未规范化** — "谁负责在 Prompt CRUD 时阻止给豆包创建 en-US 版本" 没有代码/API 合约描述
- 测试: ❌ **PRD_TEST_DATA_V1.md line 256 提到此场景但无 fixture**, 无断言测试

### 2.3 品牌多语言匹配 + Citation URL 归一化

- URL 归一化: ✅ `tldts` 强制 (ADAPTER §8.4)
- 域名归属: ✅ kg_brand_domains 查表 + alias fallback (ADAPTER §8b)
- **边界未覆盖**: m./mobile/ 子域、/zh-cn/ 语言 path、短别名歧义 (如"精英" vs "精英电子")、短字母缩写品牌 (HM/ZARA) 的大小写敏感
- **测试**: ❌ 零 fixture, 零 unit test

### 2.4 成本 & 延迟可观测性 (Frank 特别关注的细粒度监控)

**ai_responses 表当前字段** (DATA_MODEL.md + ADMIN_PRD_B §3.1.3):

| 字段 | 是否有 | 缺陷 |
|---|---|---|
| `latency_ms` | ✅ | 单值聚合, 未分段 |
| `cost_usd / cost_cny / token_count` | ❌ | **完全缺失** — 无法回答"本周哪个引擎最贵" |
| `proxy_connect_ms / llm_call_ms / parse_ms / db_write_ms` | ❌ | **完全缺失** — 无法回答"慢是代理还是 LLM" |
| `trigger_source (auto_scheduled/user_backfill/manual_admin_rerun)` | ❌ | 无法区分排程抓取 vs 用户回填 |

**Admin 现有监控盲点**:
- 每引擎成本分布: ❌ 无 schema 支持
- 每代理节点 CAPTCHA 率: ⚠️ 部分 (§2.3 仅 by engine, 非 by proxy_region)
- 每帐号失败类型分布: ⚠️ health_score[0-100] 聚合, 非 by-error-type
- Parse 失败按 DOM selector 聚类: ❌ 无分析工具 spec

### 2.5 代理 & 帐号池可靠性

| 项 | 状态 |
|---|---|
| Ninja Clash 订阅刷新: 6h cron | ✅ |
| 新节点 healthCheck 先于加入池 | ✅ |
| **0 健康节点 fallback**: in-flight Query 如何处理? | ❌ 未指定 |
| **订阅 API 不可达时回退策略** | ❌ 未指定 |
| 帐号 pre-warm (新帐号加入 ACTIVE 前) | ❌ 未指定 |
| Cookie 保活 2h + SELECT FOR UPDATE SKIP LOCKED | ✅ |
| consecutive_failures ≥ 3 → BANNED | ✅ |

### 2.6 必补 schema (P0, 用于细粒度监控)

```sql
ALTER TABLE ai_responses
  ADD COLUMN cost_usd NUMERIC(10,6),
  ADD COLUMN cost_cny NUMERIC(10,4),
  ADD COLUMN prompt_tokens INT,
  ADD COLUMN completion_tokens INT,
  ADD COLUMN latency_breakdown JSONB,
      -- { proxy_connect_ms, llm_call_ms, parse_ms, db_write_ms }
  ADD COLUMN trigger_source TEXT
      CHECK (trigger_source IN ('auto_scheduled','user_backfill','manual_admin_rerun'));

CREATE TABLE kg_mined_relations (
  id UUID PRIMARY KEY,
  source_entity_type TEXT,   -- 'brand' | 'product'
  source_entity_id UUID,
  target_entity_id UUID,
  relation_type TEXT,
  confidence NUMERIC(3,2),
  evidence_count INT,
  evidence_sources JSONB,    -- [{response_id, citation_id, excerpt}]
  status TEXT DEFAULT 'candidate', -- 'candidate' | 'promoted' | 'rejected'
  promoted_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);
-- Promotion rule (to be wired into mining job):
-- confidence >= 0.85 AND evidence_count >= 5 → status='promoted'
```

---

## 3. 维度三 · 边界异常覆盖

### 3.1 Auth & Session (PRD §4.1.1e / §4.1.1-gate)

| 场景 | PRD 覆盖 | 代码 |
|------|----------|------|
| 多 Tab 登出同步 (BroadcastChannel) | ✅ 全规 | ❌ 无 `<SessionExpiredModal>` 组件 |
| Silent refresh (15min/30d rotation) | ✅ 全规 | ❌ 无 Axios interceptor 实现 |
| 品牌直链未登录 → `/register?redirect=&brandHint=` | ✅ 全规 | ❌ 无 Route Guard |
| Onboarding 草稿 72h 保存 | ⚠️ **存储后端未指定** (sessionStorage? Redis? Supabase table?) | ❌ |

### 3.2 Pipeline 异常 (ADAPTER_CONTRACT §6.1 八错误码)

| 错误码 | Admin 可见 | Action | 语义问题 |
|--------|-----------|--------|----------|
| CF_BLOCKED | ✅ | Proxy 轮换 | ✅ |
| COOKIE_EXPIRED | ✅ | 帐号 COOLDOWN | ✅ |
| CAPTCHA_REQUIRED | ✅ | 3 级重试→Admin 队列 | ⚠️ **Level 1/2/3 失败阶段未在 Attempt 详情展示** |
| PAGE_CRASHED | ✅ | 常规重试 | ✅ |
| PROXY_DEAD | ✅ | 自动拉黑 | ✅ |
| NO_ACCOUNT_AVAILABLE | ⚠️ | "不计入成功率" | **A2.2 未明确**: Query.status=PENDING (资源约束) vs FAILED (错误) 的区分; Attempt list 默认过滤/不创建 |
| EXTRACT_EMPTY | ✅ | 3 连 DOM drift P1 告警 | ✅ |
| TIMEOUT | ✅ | 指数回退 | ✅ |

**CAPTCHA 3 级 → 人工队列**: PRD 有但 Attempt 详情页缺"哪一级失败" + "标记 profile 污染" 按钮

### 3.3 Data Integrity

| 场景 | PRD 规格 | 代码契约 |
|------|----------|----------|
| mentionRate 分母 = 0 | ✅ 渲染 "—" | ❌ 无运行时断言 (无 scripts/check-data-contracts.mjs) |
| "其他" 片 > 任何真实品牌 | ✅ C3 契约 | ❌ 无断言 |
| sentiment 0.5 边界 (positive vs neutral) | ❌ **无 tiebreak 规则** | — |
| Brand merge → 历史 Citation 归属 | ⚠️ "audit trail" 有但删除 vs. 重归属未选 | — |
| 成本 spike (>2× 日预算) 告警 | ❌ **PRD 搜"成本告警" = 0 match** | — |

### 3.4 UI 状态

| 页面 | Empty | Loading | Error Boundary | Filter URL state |
|------|-------|---------|----------------|------------------|
| Dashboard/BrandOverview | ✅ (§4.6-IA-v2.F → /onboarding) | ❌ | ❌ | ✅ |
| Visibility / Sentiment / ... | ⚠️ "0 for selected filters" 未本地化 | ❌ | ❌ | ✅ (§K FilterBar) |
| Topics | ⚠️ 模糊 | ❌ | ❌ | ✅ |

### 3.5 Onboarding 首运行

- Zero-Project Route Guard (§4.6-IA-v2.F): ✅ 规格 / ❌ 代码未实现
- 用户选未播种 KG 数据的行业: ❌ **graceful fallback 未规**
- 草稿中止 72h 恢复: ⚠️ 存储后端未指定

### 3.6 mock.js C3/C7 契约违反 (verified)

Spot-verify `BRANDS[]`:
```
panoScore: 85 → ranking 1  ✅
panoScore: 82 → ranking 2  ✅
panoScore: 79 → ranking 3  ✅
panoScore: 75 → ranking 5  ❌ (应为 4)
panoScore: 71 → ranking 7  ❌ (应为 5)
panoScore: 68 → ranking 8  ❌ (应为 6)
panoScore: 77 → ranking 4  ❌ (应为 4, 且下面有 75/73)
panoScore: 73 → ranking 6  ❌ (应为 6, 顺序混乱)
```
按 panoScore DESC 排序实际应为 `85,82,79,77,75,73,71,68` → ranking `1..8`, 当前 mock 打乱. **PRODUCTS[]** (12 条) spot-verify 通过 ✅.

**→ 需修 BRANDS ranking 字段 + 落地 `scripts/check-data-contracts.mjs` 做 C3/C7 运行时断言**.

---

## 4. 维度四 · 测试场景齐全

### 4.1 4 层架构覆盖 (TEST_STRATEGY §2)

| 层 | 规格 | 仓库实现 | 结论 |
|----|------|----------|------|
| L1 Harness | 30+ grep (i18n / C1-C7 / C9-C15 / UI boundary) | `scripts/ci-check.mjs` ❌ 不存在; `.husky/pre-commit` ❌; `.github/workflows/ci.yml` ❌ | 纸面规则, 0 执行 |
| L2 Unit (Vitest) | ~200 tests | `vitest.config.*` ❌; `frontend/tests/` ❌; `package.json` 无 test script | 0 测试 |
| L3 Contract + HAR | ~80 契约自动生成 + HAR replay | `openapi.yaml` ✅ 1557 行; `scripts/gen-api-tests.ts` ❌; `fixtures/scraping/` ❌ | schema 有, 生成器未建 |
| L4 E2E + Visual | 6 关键路径 + ~40 baseline | `playwright.config.*` ❌; baseline ❌ | 0 |

**package.json scripts (spot-verified)**:
```json
{ "dev": "vite", "build": "vite build", "preview": "vite preview" }
```
缺 `test:unit / test:integration / test:e2e / check:harness`.

### 4.2 Harness 实现状态 (spot-verified)

项目根目录 `scripts/` / `.husky/` / `.github/` 三个目录 **全部不存在**. CLAUDE.md 里列出的所有 Harness grep (C1-C7 图表契约、C9-C15 V2 分析页契约、i18n 开发约束拦截、UI 边界拦截) 均为"aspirational engineering standard", **零条真实生效**.

### 4.3 Admin 测试缺口

- CAPTCHA 队列审核 UI: ✅ specced / ❌ 0 test
- 帐号 quarantine 流: ✅ specced / ❌ 0 test
- KG 合并审计日志: ✅ specced / ❌ 0 test
- Pipeline 重试 workflow E2E: ✅ specced / ❌ 0 test (Admin frontend 代码全无, E2E 必然 0)

### 4.4 mock.js 测试 fixture 缺口

缺失的 fixture:
- `TOPICS=[]`, `RESPONSES=[]`, `ALERTS=[]` (empty state)
- `ERROR_RESPONSE = { status:'failed', error_code:'DOM_DRIFT' }` 等 (ADAPTER §6.1 全 8 错误码 mock)
- `isLoading: true` 骨架态

---

## 5. 排序问题清单 (Top 20, 按修复收益排)

| # | P | 维度 | 问题 | 建议 Session | 工时估 |
|---|---|------|------|--------------|--------|
| 1 | P0 | 测试 | `scripts/ci-check.mjs` + pre-commit + CI workflow 建立 | **Session 0 (新建)** | 2h |
| 2 | P0 | 测试 | `vitest.config.ts` + `playwright.config.ts` + `package.json` 4 test scripts | Session 0 | 3h |
| 3 | P0 | App frontend | brand/ 缺 4 页 (Overview / Topics / Diagnostics / Reports) | **T2'** 执行/续 | 8-12h |
| 4 | P0 | App frontend | industry/ 缺 1 页 (KnowledgeGraph) + KnowledgeGraphPage 301 | **T3'** 执行/续 | 3h |
| 5 | P0 | App frontend | 布局组件 ModeToggle / BrandPicker / IndustryPicker + DashboardLayout 重写 | **T1'** 执行/续 | 6h |
| 6 | P0 | App frontend | 13 个 legacy top-level 页 301/删除 | T1' 尾部 | 2h |
| 7 | P0 | Admin | Citation Tier CRUD + MCP Token 签发/撤销 — 新建 Session A5 | **Session A5 (新建)** | 6h |
| 8 | P0 | Pipeline | Intent Matrix 决策表 (dimension × industry → Intent[]) 写入 PRD §4.10.3.A | PRD 增补 | 1h |
| 9 | P0 | Pipeline | `ai_responses` 增 cost / token / latency_breakdown / trigger_source 字段 | **Session 2 Prisma 迁移补丁** | 1h |
| 10 | P0 | Pipeline | `kg_mined_relations` 表 + mining job + promotion 规则 | **Session 3 / 新 A3.4** | 6h |
| 11 | P0 | Admin | A2.2 Prompt 补"admin sandbox HAR routeFromHAR 回放" | ADMIN_SESSIONS 补丁 | 1h |
| 12 | P1 | Admin | A3 增 Trust Score 系统 (C5) + Hallucination 检测 (C6) | ADMIN_SESSIONS A3 scope 扩充 | 8h |
| 13 | P1 | Pipeline | 0 健康代理 + 订阅不可达 fallback 显式化 | ADAPTER_CONTRACT §7 补 §7.4 | 30min |
| 14 | P1 | Pipeline | 帐号 pre-warm / ACTIVE 前 healthCheck 流 | ADAPTER_CONTRACT §5.3 新增 | 1h |
| 15 | P1 | Admin | NO_ACCOUNT_AVAILABLE → Query.status=PENDING 与 Attempt list 过滤规则 | A2 Prompt + ADAPTER §6.1 互引 | 1h |
| 16 | P1 | 边界 | Onboarding 草稿 72h 存储后端选型 (Supabase table 推荐) | PRD §4.1.1d.C 补 | 30min |
| 17 | P1 | 边界 | Sentiment 0.5 tiebreak 规则文档化 | PRD §4.2.x 1 行 | 15min |
| 18 | P1 | 边界 | 成本 spike 告警 (>2× 日预算) — 新 §4.9.x 节 | PRD 补 | 1h |
| 19 | P1 | 测试 | mock.js BRANDS[] ranking 修正 + `scripts/check-data-contracts.mjs` 落地 | Session 0 | 1h |
| 20 | P1 | i18n | messages.js 拆为 7 namespace + `formatBrand` utility | **Session 5 前置** | 4h |

---

## 6. Session 级修复建议 (插入到现有 SESSIONS 里)

### 建议新增 Session 0 (在 T1' 前)
目的: 补齐测试地基 (问题 #1, #2, #19)
产出:
1. `scripts/ci-check.mjs` (合并 C1-C7 + C9-C15 + i18n 泄漏 + UI 边界 30+ 条 grep)
2. `scripts/check-data-contracts.mjs` (C3 其他 ≤ any real; C7 ranking 按 panoScore DESC)
3. `vitest.config.ts` + `playwright.config.ts`
4. `.husky/pre-commit` (staged lint + ci-check)
5. `.github/workflows/ci.yml` (ci-check + unit + contract + e2e, 12min 预算)
6. `package.json`: `test:unit / test:integration / test:e2e / test:visual / check:harness / check:contracts`
7. mock.js BRANDS[] ranking 修正 + 新增 `EMPTY_STATE_FIXTURES` / `ADAPTER_ERROR_FIXTURES`

### 建议新增 Session A5 (在 A3 后)
目的: 闭环问题 #7, #11 (Admin 侧决策 #19 的 Tier + MCP token 签发)
产出:
1. `/admin/api/v1/citation-tiers` CRUD + 5 级 Tier 种子数据 seed script
2. `/admin/api/v1/mcp-tokens` issue / list / revoke + withAudit
3. A5 前端: CitationTier 表格 + MCP Token 列表 (Radix Dialog 新建/撤销)
4. Admin HAR routeFromHAR 沙盒回放 (合并自 A2.2 补丁)

### 建议 T1'/T2'/T3' 续执行
- T1': DashboardLayout 重写 (Mode-aware) + ModeToggle + 13 legacy 301
- T2': 补 BrandOverview / BrandTopics / BrandDiagnostics / BrandReports 4 页
- T3': 补 IndustryKnowledgeGraphPage (可从 legacy KnowledgeGraphPage 迁移 G6 逻辑)

### 建议 Session 2 Prisma 迁移补丁
- ai_responses 增 4 字段 (见 §2.6 SQL)
- 新建 `kg_mined_relations` 表
- prompt_templates 若无 `applies_to_engines` 约束则加 check

### 建议 PRD 增补
- §4.10.3.A: Intent Matrix 决策表 (每行业每 dimension 的 default intents)
- §4.1.1d.C: 草稿 Project 72h 存储后端 = Supabase `draft_projects` table
- §4.2.x: Sentiment tiebreak 规则 (0.5 归 neutral, >0.5 positive, <0.5 negative)
- §4.9.x: 成本 spike 告警规则 (2× 日预算 P1, 5× 日预算 P0)

### 建议 ADAPTER_CONTRACT 增补
- §5.3: 帐号 pre-warm 流 (新帐号 → warmup homepage + /api/user/me 3 次成功 → 进 ACTIVE)
- §7.4: 0 健康节点 / 订阅 API 不可达 fallback (in-flight Query 暂停 30min; 广播 P0 告警)

### 建议 ADMIN_SESSIONS 增补
- A2.2 Prompt: HAR Tab 增 "回放" 按钮 + headless Playwright + `routeFromHAR()` + 结果可视化
- A2.3 Prompt: change_type enum 增 `kg_entity_merge / kg_entity_split / kg_brand_approve / kg_relation_confidence_change`
- A2.4 Prompt: QA 每日抽样 200 条, 按 engine × industry 分层比例分配, cron 04:00
- A3 Prompt: 补 C5 Trust Score 系统 + C6 Hallucination 检测

---

## 7. 风险结语

1. **pipeline mining 是战略盲点**: KG 持续迭代是 GENPANO "数据护城河"的核心机制, 但促进规则、schema、job 全无. 若 Session 3 开写 Pipeline 再补, 会破坏 KG 初始化与运行数据的一致性.

2. **测试基建不先行 = 所有 Harness 只是注释**: 当前 CLAUDE.md 30+ 条 grep 读起来很严密, 实际 0 执行. 每个 Session 都会"绕过"这些约束, PR 合入后需要返工.

3. **Admin 完全未开工**: 所有 A1-A4 Session 都停在规格层. Frank 如果先跑 App (T1'-T5'), Admin 侧 pipeline 无人管, 数据流不起来也就看不到 App 面板. 建议与 App 并行推 A0-A2.

4. **Frank 的决策沉淀速度 > Session 消化速度**: 决策 #19 (Citation Tier + MCP token) 和 #20 (V2 分析页 + Wave-4) 都是近 5 天定的, 却没有 Session 承接. 建议建立 "决策→Session 增补" 的例行同步 (每 3 个决策触发一次 SESSIONS diff).

---

**报告终**
