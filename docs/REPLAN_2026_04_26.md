# GENPANO Replan 2026-04-26 · Python Pivot + Milestone-Driven Sessions

**作者**: Frank
**日期**: 2026-04-26
**触发**: 合并仓 https://github.com/jotamotk/GenPano.git (Frank 把 LLM scraper 实验代码合并进来, 含 geo_tracker/ Python ~10000 行) 验收后, 重新规划 PRD/Session

> **真相源声明 (Frank 2026-04-26 明确指示)**: 合并仓里的 `PRD.md` 版本不对, **不必参考**。本文档以及所有后续 Session 的真相源仍然是当前 GENPANO_Claude_Lead 仓库下的 `docs/PRD.md` (master, 425KB) 与 `CLAUDE.md` 决策 #1-#28。合并仓只贡献 `geo_tracker/` (Python 实战代码) 与 `frontend/` (TSX Auth 8 页面) 两个**实施层资产**, PRD/SESSIONS/ADAPTER/DATA_MODEL 等战略层文档**全部以本仓库为准**。
**关联决策**: CLAUDE.md #28' (Python pivot) + #28'' (preview env + 可验证前后端)
**状态**: ⚠️ Active — supersedes 部分 master Session 0/A0/1/1.5/2/2.1 实施层

---

## 1. 决策概要 (Executive Summary)

| 维度 | master 路线 (作废) | 本次决策 (生效) |
|---|---|---|
| **后端栈** | Next.js 14 App Router + Prisma + TS | **FastAPI + SQLAlchemy 2.0 async + Alembic + Celery + Redis + python-jose + slowapi** |
| **前端栈** | Next.js + JSX + Tailwind + next-intl | **React 18 + Vite + TSX + Tailwind + i18next** |
| **真相源** | docs/PRD.md (425KB) | **不变 — master PRD (本仓库) 仍是战略层唯一真相源; 合并仓 PRD.md (29KB v1.2) Frank 明确指示不必参考** |
| **MVP 引擎** | ChatGPT + Doubao + DeepSeek-CN | **不变 (3 家)** |
| **MVP 行业** | 美妆/奢侈品/食品饮料/服装时尚 | **不变 (4 个)** |
| **Session 编号** | Session 0/A0/1/1.5/2/2.1 (后端 TS) | **Session 0'/A0'/1'/1.5'/2'/2.1'/1.2'/3'/A1'/4a'/4b' (Python 重写)** |
| **节奏** | 每 Session 局部交付 | **Milestone 驱动 — 每 2-3 Session 凑成 1 Milestone, 每 Milestone 末必交付 preview deploy + 端到端可验证** |
| **MVP 时长** | 4 周 (原 PRODUCT_PLAN) | **8-10 周 (4 Milestone × 2-3 周)** |

**核心反转**: Frank 决策 "目前 session 都是测试的, 没有正式上生产, 所以期望做出来是最优的架构设计" → 沉没成本让位给架构纯粹性 → 后端单一 Python 生态, master 7 个 TS Session (Session 0/A0/1/1.5/2/2.1 + Session 1 双修正预先登陆) 后端代码作废, 前端设计逐步迁移。

---

## 2. 架构反转理由

### 2.1 Python 不可替代的部分 (合并仓 geo_tracker/, ~10000 行)

| 模块 | 文件 | 不可替代理由 |
|---|---|---|
| **Camoufox 反指纹浏览器** | agent/executor.py + guest_executor.py | playwright-extra stealth 在 TS 生态弱一档, Camoufox 是 Firefox fork, 无 TS 等价 |
| **Slider CAPTCHA 解算** | agent/slider_captcha.py 454 行 | OpenCV + numpy 算法栈, TS 端没有等价库 |
| **Vision CAPTCHA (ARK)** | agent/vision_captcha.py 388 行 | 火山 Ark API 调用 + PIL 图像处理 |
| **鲁班 SMS 自动注册** | sms_login/luban_client.py | Python HTTP client + retry, 已跑通 doubao/deepseek 注册流程 |
| **Clash proxy 切换** | agent/clash_api.py | 海外代理切换, V-Ninja Clash API |
| **NLP 分析 pipeline** | analyzer/llm_analyzer.py + brand_detector.py + sentiment_analyzer.py + citation_mapper.py | 火山 Ark API + json-repair 已跑通 7 引擎实战 |
| **Profile-aware 扇出** | generation/fanout_engine.py 284 行 | profile 采样 + 扇出已跑通 |
| **Account/Proxy Pool** | pool/account_pool.py + proxy_pool.py | 已跑通账号轮换 + 代理黑名单 |

### 2.2 master TS 后端的不可挽留

- Session 0/A0/1/1.5/2/2.1 共交付 ~10000 行 TS + 727 vitest + 38 grep harness, 但**核心爬取和分析逻辑必须重写为 Python**
- 混合栈代价: 双栈数据契约 (Prisma + SQLAlchemy 双 schema 定义) / 双栈 ORM / 双 CI / 双 Harness / 双 Test runner — 长期维护成本超过纯 Python
- 沉没成本: 战略层文档 (425KB PRD + 决策 #1-#28 + ADAPTER_CONTRACT + DATA_MODEL + TEST_STRATEGY + REVIEW_2026_04_21) **全部保留**, 仅实施层 (Prisma schema + TS Adapter framework + TS Vitest + TS Harness) 重写

### 2.3 前端 — 合并仓已合并 master 设计稿 (重要纠偏 2026-04-26)

> **2026-04-26 实地核查 (`C:\Users\frank.wang\genpano` 挂载后) 修正**: 此前盘点声称"合并仓 frontend 是 TSX 8 页面 Auth", 经核查**这是错读**。实际情况:

- **合并仓 `claude/merge-frontend-pages-DWFMx` 分支** (当前活跃分支) 已经完成"把 master frontend 设计稿合并到合并仓"的工作 — 该分支名本身就说明这一点
- frontend 是 **JSX (不是 TSX)**, 21 个 page + brand/ 子目录 5 个 (BrandCitationsPage / BrandCompetitorsPage / BrandProductsPage / BrandSentimentPage / BrandVisibilityPage), 跟 master `GENPANO_Claude_Lead` 仓库的 frontend 设计稿**几乎是同一套**
- 合并仓 `frontend/src/admin/pages/` 已有 4 个 Admin 页面 (Login / ForgotPassword / ChangePassword / Dashboard), 跟 master Session A0 已交付的 Admin 前端**对齐**
- 合并仓 `frontend/src/` 子目录: admin/ + components/{auth,brand,charts,citation,commercial,dashboard,dev,diagnostics,empty,filters,industry,landing,topics,ui} + contexts/ + data/ + hooks/ + i18n/ + layouts/ + lib/industry/ + pages/{brand,industry} — IA v2.0 全套已就位
- 合并仓 frontend `package.json` 已含 React 18.3 + Vite + Tailwind + react-router-dom + recharts + d3 + framer-motion + lucide-react + mixpanel-browser + react-hook-form + zod + @antv/g6 + @radix-ui — **stack 与 master 一致**, 不是"全新"前端

**修正后的处置**:
- Session 0' 起点 = **合并仓 `claude/merge-frontend-pages-DWFMx` 分支当前 frontend** (JSX + 21 page + IA v2.0 全套, 已经是 master 设计稿)
- TSX 改造 (JSX → TSX) **不是必须**, MVP 内可以保持 JSX (节省工作量); 若决定迁 TSX 是 Phase 2 工作 (类型安全增强), 不阻塞 MVP
- 真实 API 接通 (mock.js → FastAPI) 仍然是 Session 4b' 工作, 但工作量比原估**显著降低** (因为 master 设计稿已经在合并仓里, 不需要二次翻译)

> **决策影响**: Session 4b' 工作量从"~50+ 页面 JSX→TSX 翻译 + mock→real API"**降级**为"mock.js → FastAPI 接通 + 部分组件按需 TSX 改造", Milestone M4 时间预算可能从 2-3 周缩到 1.5-2 周。

---

## 3. PRD 更新清单 (master PRD 425KB 哪些段落需要吸收 geo_tracker 经验)

> 战略层不变, 仅实施层段落需要明确"Python 实现"语义。
>
> **不参考合并仓 PRD.md**: Frank 2026-04-26 明确指示合并仓 PRD 版本不对, 本表所有更新依据是 (a) 当前仓库 `docs/PRD.md` 的现有段落 + (b) 合并仓 `geo_tracker/` Python 代码反映的实战经验 + (c) `CLAUDE.md` 决策 #1-#28, **不引用**合并仓 PRD/PRD-auth.md/scraping-experience-report.md 等任何 .md 文档。

| PRD 段号 | 状态 | 更新内容 |
|---|---|---|
| §4.1 Auth | **沿用现有 §4.1 Email-first 2-step (决策 #15)** | 实施层换为 Python (FastAPI + python-jose + passlib + slowapi); 自动注册流程接纳 geo_tracker/sms_login/ 实战逻辑 |
| §4.2.6 Citation | **不变** | 5 Tier 权重表 / PANO A 公式 / 全部保留 |
| §4.2.7 Citation 行动面 | **不变** | 6 条行动面 (归因诊断 / 内容策略 / 外联 PR / 竞品解构 / Simulator / MCP) |
| §4.3 Adapter Contract | **吸收 Camoufox + Cloudflare Turnstile + slider/vision CAPTCHA 实战经验** | ADAPTER_CONTRACT.md §5 (账号状态机) / §5.3a (Pre-Warm 7 步) / §5.4 (CN 引擎自动注册) / §7 (CAPTCHA 三级) 内容仍然有效, 需明确实现栈 = Python (Camoufox + PIL + 火山 Ark) |
| §4.4 Pipeline | **吸收 Celery 路由设计** | 6 队列 (llm_chatgpt / llm_doubao / llm_deepseek / analysis / account_login / beat) 写入 §4.4.X |
| §4.5.2 MCP API | **不变** | 3 个工具 (genpano_get_citations / list_pr_targets / simulate_authority_boost) 由 FastAPI 承载, 不影响 schema |
| §4.6-IA v2 | **不变** | Brand/Industry Mode pill toggle / 9+4 子页面 / Onboarding 4 步 / Route Guard → /onboarding |
| §4.10.3.A | **不变** | Intent × Engine × Locale 23 行决策矩阵 (本表的 Python 实现是 Session 2' 任务) |
| §1.9 KG | **不变** | kg_mined_relations 8 种关系 + confidence_score = min(1, 1 - 0.85^evidence_count) |
| §2.5 ai_responses | **吸收 6 枚举 response_source** | web_ui / api_fallback / mock_proxy / cached_replay / admin_har_replay / harness_fixture (NO SCHEMA DEFAULT) |
| §5.6 Admin Auth | **不变** | AdminUser / AdminSession / AdminPasswordReset / AdminLoginAttempt |

**关键不变项**: 决策 #1-#28 战略层全部保留 (含 #9 Auth-Required / #10 Onboarding / #15 Report 框架 / #16 提及率 non-brand / #19 Citation 全链路 / #20 V2 分析页 / #21 Review 修复闭环 / #22 Adapter 框架契约 / #23 KG 冷启动 / #24 Session A0 Auth / #25 Session Prompt 公约 / #26 Session 2 Planner / #27 Session 2.1 LLM Refinement)

---

## 4. Session 序列 (11 Session, Python 重写)

> 命名规则: master Session 编号末尾加单引号 `'` 标识 Python 重写。
> 顺序原则: 基础设施 → Auth 端到端 → Pipeline → Admin 监控 → 用户态 → IA v2.0 完整。

### Session 0' · 工程基线 + CI/CD 流水线 + Preview Env (基础设施, 大量复用合并仓现有产出)

> **2026-04-26 实地核查发现**: 合并仓已经搭了相当多的 preview 基础设施 (`docker-compose.preview.yml` + `nginx-preview.conf` + `query-tool-preview` + `frontend-preview` 服务 + `.github/workflows/{deploy-preview.yml, deploy.yml, docker-cleanup.yml}`), Session 0' 不是从零搭, 而是**把现有 preview infra 整理 + 补齐到符合 11 Session 编排所需**。

**范围 (做)**:
- Monorepo 结构整理: backend (Python, 重组 geo_tracker/ → app/ structure) + frontend (复用合并仓 JSX, 不强制改 TSX) + docs (引入 master `docs/PRD.md` 425KB + 决策 #1-#28 真相源) + scripts
- Python 工具链: `pyproject.toml` (uv) + ruff + mypy strict + pytest + pytest-asyncio + pre-commit hook
- FastAPI skeleton: `app/main.py` + `/healthz` + CORS — 注: 合并仓现有 `query_tool/app.py` 是 **Flask** 的 30+ route Admin Pipeline UI, **不是 FastAPI**; Session 0' 决定: (a) Flask 保留作为现网 Admin Pipeline 监控 UI (低优先级迁移); (b) 新功能用 FastAPI; (c) 共享同一 PostgreSQL DB; (d) Phase 2 把 Flask 路由迁到 FastAPI
- SQLAlchemy 2.0 async + Alembic 初始化 — 注: 合并仓现有 `migrations/001_analyzer_tables.sql` (PR #104, 2026-04-09) 是手写 SQL, 不是 Alembic; Session 0' 决定: 用 Alembic 接管 (`alembic init` + 把 001_analyzer_tables.sql 反向工程为 Alembic 第一个 revision)
- Frontend: 复用合并仓现有 JSX 21 页面, Vite + Tailwind 不动; 加 `VITE_API_BASE_URL` env 区分本地/preview/prod
- **CI/CD 流水线扩充** (基于现有 .github/workflows/, 不重写):
  - 现有 `deploy-preview.yml` 扩为完整 PR-on (lint + pytest + vitest + build + Render/Vercel deploy + 自动评论 PR URL)
  - 现有 `deploy.yml` 改为 main-on prod 部署
  - 新增 `ci.yml` PR-on 必过 (Harness selftest + harness checks)
- 环境变量整理: 合并仓 `.env.example` 4355 字节已存在, 补齐 preview/staging/prod 三套
- Harness 第 1 批 Python 化: F1 (no-bare-playwright-import) / F4 (response_source labeling) / D8 (no-hardcoded-jwt-secret) — 用 ruff 自定义规则 + AST visitor 实现, 自验证 fixture 5 条

**范围 (不做, 推到后续 Session)**:
- JSX → TSX 大改造 (Phase 2)
- Flask query_tool/ 迁 FastAPI (Phase 2)
- 38 条 grep harness 全部 Python 化 (本 Session 只做 3 条最关键的, 其余分散到 Session 1'/A0'/2' 等具体引入这些规则的 Session)

**依赖**: 无 (起点)
**关系到 master**: 替代 master Session 0; 大量复用合并仓现有 preview infra 而不是重新搭
**Phase Gate**:
- Frank 在浏览器打开 `https://<preview-domain>/preview/` (合并仓 nginx-preview 已配 /preview/* 路由) 看到 frontend
- Frank 在 `https://<preview-domain>/api/healthz` 看到 FastAPI `{"status":"ok"}` (新加的 FastAPI sub-app)
- Frank 在 `https://<preview-domain>/preview/admin` 看到合并仓现有 query_tool Flask UI (account pool / queries / analyzer stats 等已有功能)
- `pre-commit run --all-files` 全绿
- `pytest` 全绿 (≥ 1 healthz 测试)
- `npm run ci` 全绿 (合并仓现有 ci 命令, 含 check:harness + check:contracts + check:selftest + test:unit)
- GitHub Actions PR check 全绿

### Session A0' · Admin 认证脚手架 (Python 重写)

**范围**:
- SQLAlchemy 模型: AdminUser / AdminSession / AdminPasswordReset / AdminLoginAttempt (对齐 ADMIN_PRD §5.6.8 真相源)
- python-jose JWT (HS256, 同 secret 管理) + passlib bcrypt (cost 12)
- FastAPI router `app/admin/auth/`: login / refresh / logout / forgot-password / reset-password / change-password
- slowapi rate limiter (5次/15min email + 20次/15min IP)
- middleware (FastAPI dependency): `require_admin_session()` + `require_recent_auth()`
- Cookie: HttpOnly + SameSite=Strict + Path=/admin + Secure (prod)
- Admin frontend: `frontend/src/admin/` 4 页面 (Login / ForgotPassword / ChangePassword / Dashboard) + AdminAuthContext + SessionExpiredModal + AdminRouteGuard + adminApi.ts
- Bootstrap seed: 幂等 `scripts/admin-bootstrap.py` 创建 super_admin
- Harness D8/D9/D10 Python 重写

**依赖**: Session 0'
**关系到 master**: 替代 master Session A0 (TS → Python, AdminUser 字段名/CHECK 约束严格对齐 ADMIN_PRD §5.6.8)
**Phase Gate**:
- Frank 在 preview URL `https://genpano-preview.vercel.app/admin/login` 用 super_admin 邮箱登录
- 登录后 silent refresh 14min 触发自动续签 (Frank 浏览器开 30min 不掉线)
- 5 次错密码触发 429
- pytest admin/auth 覆盖率 ≥ 80%

### Session 4a' · 用户 Auth + Onboarding 4 步 (吸收合并仓 frontend)

**范围**:
- SQLAlchemy 模型: User / Project / DraftProject (对齐 PRD §4.1.1d.C 草稿 72h)
- FastAPI router `app/api/v1/auth/`: register / verify-email / login / forgot-password / reset-password
- Resend + React Email 双语模板
- Frontend: 合并仓 LoginPage/RegisterPage/EmailSentPage/SetupPage 已实现, 接通 FastAPI 真实 API (替换 mock)
- Onboarding 4 步: 选行业 → 选主品牌 → 选竞品 → 偏好 (Route Guard 强制重定向 /onboarding)
- 草稿 72h 过期 + pg_cron 清理任务 (Celery beat)
- 埋点 #63/#64/#65 + #70 onboarding_step_completed

**依赖**: Session 0' + A0' + 1.5' (KG 数据准备, 行业/品牌选择需要 KG)
**关系到 master**: 替代 master Session 4a (Onboarding 部分由合并仓现有 frontend 已起步)
**Phase Gate**:
- Frank 注册新账号 → 收 verify email → 点击链接 → 落到 /onboarding → 4 步走完 → 落到 /brand/overview (空数据)
- 草稿测试: 第 2 步退出 → 重新登录 → Route Guard 跳回 /onboarding step 2

### Session 1' · Adapter 框架 + Account/Proxy Pool (Python 重写, 吸收 geo_tracker)

**范围**:
- 复用合并仓 `geo_tracker/` 实现, 重新组织为 `backend/app/engines/`:
  - `adapters/{chatgpt,doubao,deepseek}/` (web + api fallback) — 直接接纳 executor.py + guest_executor.py 已跑通逻辑
  - `parsers/{normalize,brand_matcher,sentiment_classifier,citation_extractor,ranking_extractor}.py` — 重组 analyzer/ 模块
  - `scheduler/{retry,backoff,state,queue}.py` — Python asyncio 实现
  - `accounts/{state_machine,pool,prewarm,auto_register,sms/luban}.py` — 吸收 sms_login/ + pool/
  - `proxy/{pool,clash}.py` — 吸收 agent/clash_api.py
  - `behavior/humanize.py` + `captcha/{slider,vision,solve}.py` — 吸收 agent/human_behavior.py + slider_captcha.py + vision_captcha.py
- 6 枚举 `response_source` labeling (NO SCHEMA DEFAULT)
- Harness F1/F2/F3 Python 重写 (no-bare-playwright-import / har-fixture-secret-leak / no-inline-prompt-literal)
- pytest 单测覆盖率 ≥ 80% (parsers / scheduler / accounts / proxy / humanize / profile-sampler)

**依赖**: Session 0'
**关系到 master**: 替代 master Session 1 + Session 1.2 双修正预先登陆 (Session 1 TS 框架作废, geo_tracker Python 实现接纳为基线; ADAPTER_CONTRACT.md 仍是真相源)
**Phase Gate**:
- pytest backend/tests/unit/engines 全绿, 覆盖率 ≥ 80%
- Harness F1/F2/F3 selftest 通过
- (本 Session 不要求 preview 端到端验证, 因为还没接到 Pipeline 编排; preview 验证留给 Session 1.2' Milestone 末)

### Session 1.5' · KG 冷启动 (Python 重写)

**范围**:
- 复用 master Session 1.5 算法逻辑 (决策 #23), 重写为 Python:
  - `app/platform/llm/client.py` (火山 Ark wrapper, openai-compatible)
  - `app/platform/discovery/{category_tree,brand_discovery,product_discovery,dedupe}.py`
  - `app/platform/knowledge_graph/{confidence,brand_relations,product_relations,relation_extractor}.py`
  - `app/platform/scheduler/platform_scheduler.py`
  - `app/platform/seed/mvp_industries.py` (4 行业 seed)
- SQLAlchemy 模型: kg_industries / kg_categories / kg_brands / kg_products / kg_brand_relations / kg_product_relations / kg_mined_relations
- 端到端 seed script: `scripts/seed_platform_data.py` (--dry-run / --industry / --max-llm-calls)
- pytest 覆盖率 ≥ 80%

**依赖**: Session 0' + 1' (复用 brand_matcher / citation_extractor 等 parsers)
**关系到 master**: 替代 master Session 1.5 (TS → Python)
**Phase Gate**:
- `python scripts/seed_platform_data.py --dry-run --industry=beauty-personal-care` 跑通, 写入 preview DB ≥ 20 brands + ≥ 5 products
- Admin preview URL `https://genpano-preview.vercel.app/admin/kg/industries/beauty-personal-care` 看到品牌列表

### Session 1.2' · Camoufox + 3 引擎 Adapter Live + Luban SMS Live (吸收 geo_tracker)

**范围**:
- Camoufox launch (`backend/app/engines/browser/camoufox_launch.py`) — 直接接纳 geo_tracker/agent/executor.py 启动逻辑
- 3 引擎 execute() 真实化:
  - ChatGPT (Cloudflare Turnstile bypass) — 接纳 guest_executor.py
  - Doubao — `.reference-card[data-href]` citation 抽取真实
  - DeepSeek-CN — localStorage.userToken 双轨鉴权真实
- Luban SMS live + auto-register live (doubao + deepseek-CN 自动注册, ChatGPT 走 CLI 手工导入)
- 3 个 CLI 命令: `accounts:list` / `accounts:register` / `accounts:inject`
- Golden HAR 录制 + routeFromHAR 回放 (pytest-playwright)
- Harness F4 三子规则 Python 重写 + Platform Layer 边界 harness `no-luban-import-outside-accounts-dir`

**依赖**: Session 1' (Adapter 框架就位)
**关系到 master**: 替代 master Session 1.2 (TS 双修正预先登陆作废, geo_tracker live 实现接纳)
**Phase Gate (M2 Milestone 末)**:
- Frank 在 preview env 跑 `accounts:register --engine=doubao` (走鲁班) 成功入池
- Frank 在 preview env 触发 1 条 ChatGPT/Doubao/DeepSeek query, Admin 后台看到 `ai_responses` 表新增 3 行 `response_source='web_ui'`
- pytest engines/integration (HAR replay) 全绿
- Harness F4 selftest 通过

### Session 2' · Planner Pipeline (Topic → Prompt → Query, Python 重写)

**范围**:
- 复用 master Session 2 算法逻辑 (决策 #26), 重写为 Python:
  - `app/platform/planner/intent_engine_locale_matrix.py` (PRD §4.10.3.A 23 行真相源)
  - `app/platform/planner/{category_purity,topic_planner,prompt_generator,query_assembler,sample_guard,topic_pool,agent_profiles}.py`
- 3 个 Admin 只读 API: `/admin/api/v1/platform/industries/{slug}/topics` 等 (FastAPI router)
- SQLAlchemy 模型: platform_topics / platform_prompts / query_executions (attempts JSONB, 不加 persona 顶层列, 决策 #26.C1)
- Harness G1/G2/G3/G4 Python 重写
- pytest 覆盖率 ≥ 80%

**依赖**: Session 1.5' (KG 提供 brands/products/categories)
**关系到 master**: 替代 master Session 2 (TS → Python)
**Phase Gate**:
- Admin preview URL 触发 `/admin/api/v1/platform/industries/beauty-personal-care/plan/generate` (dry-run) 返回 ≥ 17 topics + ≥ 400 prompts + ≥ 2000 queries
- Harness G1-G4 selftest 通过

### Session 2.1' · Planner LLM Refinement (Python 重写)

**范围**:
- 复用 master Session 2.1 三层 LLM 增强 (决策 #27):
  - `refine_topics_with_llm` / `naturalize_prompts_with_llm` / `rewrite_queries_for_profiles`
- Canned LLM transport (CI 默认走 canned, live 走 VOLC_API_KEY)
- Alembic migration: platform_topics 加 realism_score / llm_refined_at / audit_status; platform_prompts 加 llm_naturalized_at / naturalize_confidence
- Harness H1/H2/H3 Python 重写
- pytest 覆盖率 ≥ 80%

**依赖**: Session 2'
**关系到 master**: 替代 master Session 2.1 (TS → Python)
**Phase Gate**:
- 端到端 dump: `python scripts/dump_planner_samples.py --industry=beauty-personal-care --canned` 输出 20+20+20 sample
- Frank 视觉审查 query 是否像真人提问 (Stripe-style sanity check)

### Session 3' · 分析引擎 + 用户态 API + MCP Server (Python 重写)

**范围**:
- Response 采集 (Celery worker 消费 query_executions queue, 写入 ai_responses)
- 分析 pipeline: brand_detector + sentiment + citation_extractor 真实跑 (Celery worker_analysis queue)
- 用户态 FastAPI router `app/api/v1/`:
  - `/brands/{id}` / `/brands/{id}/topics` / `/brands/{id}/citations` / `/brands/{id}/sentiment` / `/brands/{id}/competitors` / `/brands/{id}/products`
  - `/industries/{slug}` / `/industries/{slug}/ranking` / `/industries/{slug}/topics` / `/industries/{slug}/knowledge-graph`
- MCP Server (FastAPI sub-app): `genpano_get_citations` / `list_pr_targets` / `simulate_authority_boost` (Auth-Required Bearer token, 决策 #9; token 签发 + Redis 吊销黑名单由 A1' 提供, 本 Session 仅消费验证)
- Citation Tier 读路径 (DB seed 走 Alembic; Tier CRUD UI + recompute Celery 任务**已并入 A1'**, Plan J D1, 2026-04-26)
- Cost 监控告警 (PRD §4.9.4)

**依赖**: Session 2' + 2.1' + 1.2' (3 layers 都就位)
**关系到 master**: 替代 master Session 3 + Session A5 部分 (TS → Python)
**Phase Gate (M3 Milestone 末)**:
- Frank 在 preview frontend `https://genpano-preview.vercel.app/brand/overview?brandId=loreal` 看到真实 brand 数据 (从 ai_responses 聚合)
- MCP API curl `https://genpano-api-preview.onrender.com/mcp/v1/get_citations?brandId=loreal&token=xxx` 返回 JSON
- Cost 监控触发 mock alert (preview env 注入 fake threshold)

### Session A1' · Admin 用户/KG/Pipeline 监控 + Citation Tier CRUD + MCP Token 签发 (Python 重写)

**范围**:
- ADMIN_PRD §A1-A4 内容:
  - User Management (列表 / 角色升降 / 强制改密 / 审计日志)
  - KG Quality Review (审核队列 / Brand alias merge / Relation auto-promote)
  - Pipeline Monitor (Account Pool 水位 / Proxy Pool 健康 / Cost 实时 / Response source 分布)
  - Alerts Center
- **Citation Tier CRUD + MCP Token 签发 (并入自旧 A5, Plan J D1, 2026-04-26)**:
  - Citation Tier 表 5 级权重管理 UI + 回溯 recompute Celery 任务 (PANO A 公式重算, idempotent by recompute_job_id)
  - MCP Token 签发 / 列表 / 吊销 (`mcp_api_tokens` 表 + Redis pub-sub `mcp:token:revoked` channel 60s 全节点生效)
  - 面向 PRD §4.5.2 三个 MCP 工具 (`genpano_get_citations` / `list_pr_targets` / `simulate_authority_boost`) 的鉴权底座
  - Tier 表禁硬编码 (决策 #19 硬约束 + Harness E1 + 新增 J6 citation-tier-weight-no-hardcode 联防)
- Admin frontend 同步扩展 (合并仓 frontend/src/admin/ 起步, 加 `/admin/citations/tiers` + `/admin/mcp-tokens` 两页)

**依赖**: Session 3' (用户/KG/Pipeline 数据全部就绪 + Citation 数据已落库可 recompute)
**关系到 master**: 替代 master Session A1-A4 + 旧 Session A5 (TS → Python; A5 整体合入本 Session)
**Phase Gate**:
- Frank 在 preview Admin 看到 doubao 账号池水位实时变化, 看到当日 Cost 累计
- Frank 在 `/admin/citations/tiers` 修改 Tier 2 权重 0.7 → 0.8, 触发 recompute Celery 任务, PANO A 看板数值刷新
- Frank 在 `/admin/mcp-tokens` 签发 token, 立即用 curl 命中 MCP API 返回 200; 吊销后 60s 内 curl 命中 401

### Session 4b' · IA v2.0 完整 (前端翻译 JSX → TSX, 接 FastAPI 真实数据)

**范围**:
- 把 master frontend/src/** ~50+ JSX 页面翻译成 TSX, 落到合并仓 `frontend/src/`:
  - `pages/brand/{overview,visibility,topics,sentiment,citations,products,competitors,diagnostics,reports}.tsx`
  - `pages/industry/{overview,ranking,topics,knowledge-graph}.tsx`
  - `pages/onboarding/` 4 步
  - `components/` (BrandTopicHeatmap / CompetitorQuadrantChart / DonutChart / G6 KG / TopicIntentMatrix / 等)
- 接口从 `mock.js` 切到 FastAPI 真实 API
- C9-C15 Harness Python 重写 (扫 .tsx 文件)
- Playwright E2E 6 关键路径 (TEST_STRATEGY v1.1 §11)

**依赖**: Session 3' + A1'
**关系到 master**: 替代 master Session 4b (JSX → TSX, mock → real API)
**Phase Gate (M4 Milestone 末 = MVP 完成)**:
- Frank 在 preview frontend 完成端到端 user journey: 注册 → onboarding → 看 /brand/overview → 切换到 Industry Mode → 看 /industry/ranking → 导出 CSV → 调 MCP API
- Playwright E2E 6 路径全绿
- Visual regression baseline 40+ 张

---

## 5. L3/L4 测试覆盖矩阵 (单一真相源 — 11 Session 全部引用本表)

> **触发**: Frank 2026-04-26 三连问关闭 — (a) "Session 执行完了后, 可以按照 PRD 产出期望的产品?"; (b) "我 Supabase 都没给你任何东西, 为什么测试就通过了?"; (c) "测试是不是太少了?" 答案: master 7 Session 跑出 727 vitest 单测 / 96% 行覆盖, 但 L3 集成 + L4 E2E **= 0**, Phase Gate 是**虚绿** (InMemoryRepo + canned LLM transport 无外部依赖). Python pivot 不能继承这块虚绿地基, 必须每 Session 把 L3/L4 写进 Phase Gate 硬卡控.
>
> **决策框架** (Frank 2026-04-26 四选): **Hard fail Phase Gate** (L3/L4 跑不绿不让 commit/merge, 不走 soft warning) + **分散补救** (每 Python 重写 Session 在自己 scope 内补 master TS 已有测试翻译 + 新增 L3/L4, 不单开 Session R') + **6 张 Visual baseline** (MVP 关键路径覆盖, 不展开 40 张) + **11 Prompt 一次性全改** (本次同 PR 内全部改完).
>
> **执行纪律**: 各 Session Prompt §4 Phase Gate **必须**包含本表对应行 (引用 `docs/REPLAN_2026_04_26.md §5`), L3/L4 任一未跑绿, Phase Gate **拒绝合并**. CI workflow `ci.yml` (Session 0' 交付) 必须把 L3/L4 step 列为 `required` (GitHub Actions branch protection 强制必过).

### 5.1 11 Session L3/L4/Visual 矩阵

| Session | L3 集成测试 (count + 内容) | L4 E2E 测试 (count + 内容) | Visual baseline (path) | 补救测试 (Python 重写 Session 才有) |
|---|---|---|---|---|
| **0'** | **1** · Alembic upgrade head + downgrade base 双向跑通; Celery worker + Redis 启动健康; FastAPI `/healthz` 在 preview env 返回 200 | **1** · Frank 浏览器打开 preview Landing → 点 "登录" → 落到 /auth → 截图比对 | `/landing.png` | N/A (基础设施) |
| **A0'** | **1** · super_admin bootstrap 幂等; OTP rate limit (5次/15min email + 20次/15min IP); cookie HttpOnly+Strict+Path=/admin+Secure 实测 | **1** · Frank 在 preview /admin/login 用 super_admin 邮箱 → 登录 → silent refresh 14min → Dashboard | `/admin/login.png` | N/A (Python 新写, 无 master TS 测试可继承; master Session A0 vitest 66 例已废) |
| **4a'** | **2** · User register + email verify; DraftProject 72h 过期 + Route Guard 强制重定向 /onboarding step | **1** · Frank 注册新账号 → 收 verify email → 点击链接 → /onboarding 4 步 → /brand/overview (空数据) | `/onboarding/step-1.png` | N/A (Python 新写) |
| **1'** | **4** · parsers 4 件 (brand-matcher 多语言/sentiment 三档/citation 3 tier 优先级/ranking 1-50 + N>50 拒绝); state-machine 6 状态 + COOLDOWN 差异化; HAR sanitize 9 leak pattern; profile-sampler 确定性 + segmentGroup 隔离 | **0** (Adapter execute 走 TIMEOUT sentinel, E2E 留给 1.2') | — | **TS#1 → Python pytest 463+** (master Session 1 vitest 13 套测试翻译: parsers + scheduler + accounts + engines/profile-sampler + har/sanitize 全部 Python 重实现) |
| **1.5'** | **3** · 火山 Ark client (`callJson`/budget exceeded/3 模型定价); category_tree 3 级 + level-4 容错丢弃; brand_discovery 跨 category 去重 + COMPETES_WITH/SAME_GROUP 边写入 | **0** (本 Session 无 UI 端到端) | — | **TS#1.5 → Python pytest 154+** (master 132 例覆盖 llm/db/discovery/knowledge_graph/scheduler 全部翻译) |
| **1.2'** | **3** · Camoufox launch + page.goto 真实 ChatGPT/Doubao/DeepSeek; Luban SMS live 1 条注册成功 (preview Luban 沙箱); Golden HAR 录制 + routeFromHAR 回放 3 引擎契约 | **0** (E2E 留给 3' Pipeline 联通后) | — | **TS#1.2 F4 → Python F4** (response_source 三子规则 stamp/labeling 翻译) |
| **2'** | **4** · Intent×Engine×Locale 23 行矩阵穷举; category-purity 品牌名泄漏拦截; topic-planner 3 维度 + 配额 ≥40%; persona JSONB 注入 attempts.browser_profile (G3 锁定无顶层列) | **0** (Pipeline 真实跑要 3' 才接通) | — | **TS#2 → Python pytest 319+** (master 9 套 planner + golden-beauty 13 例语义锚点全部翻译) |
| **2.1'** | **3** · refine_topics_with_llm 三档分流 (approved/audit/dropped); naturalize_prompts 4 Gate (intent/brand vocab/品类 leak/confidence); rewrite_queries fallback ladder (llm→fallback_prefix→skeleton_only) | **0** | — | **TS#2.1 → Python pytest 61+** (master 65 新例覆盖 LLM threading + canned transport 全部翻译) |
| **3'** | **4** · Response 采集 (Celery worker → ai_responses 入库 + response_source labeled); 分析 pipeline (brand_detector + sentiment + citation 真实跑); 用户态 brand API 6 路径 (auth-required); MCP Bearer token 验证 + 吊销 60s 生效 | **1** · Frank 在 preview /brand/overview?brandId=loreal 看到真实数据 (KPI 5 张 + 趋势图) | `/brand/overview.png` | **TS#3 → Python pytest 267+** (master Session 3 测试 + Citation §4.2.6/§4.2.7 全链路 Python 重写) |
| **A1'** | **2** · Citation Tier CRUD + 修改 Tier 2 权重触发 recompute Celery 任务 + PANO A 数值刷新 (idempotent by recompute_job_id); MCP Token 签发 + Redis pub-sub `mcp:token:revoked` 60s 全节点生效 | **1** · Frank 在 preview /admin/citations/tiers 改 Tier 2 0.7→0.8 → 看 PANO A 重算 → /admin/mcp-tokens 签发 token → curl 200 → 吊销 → 60s 后 401 | — | **TS A1+A5 → Python** (master 旧 A5 整体并入 A1', Plan J D1) |
| **4b'** | **3** · TSX 编译产物零类型错误 (mypy strict 等价); mock.js → FastAPI 真实 API 接通 9 + 4 页面无 broken state; CSV 导出 8 exportType 字段字典齐备 | **1** · Frank 端到端: 注册 → onboarding → /brand/overview → 切 Industry Mode → /industry/ranking → 导出 CSV → 调 MCP API (Playwright Python E2E) | `/brand/competitors.png` + `/industry/overview.png` | **TS#4b → TSX + E2E** (master frontend 50+ JSX 翻译为 TSX + Playwright 6 路径) |

### 5.2 累加验算

- **L3 集成测试**: 1 + 1 + 2 + 4 + 3 + 3 + 4 + 3 + 4 + 2 + 3 = **30 项** (≥30 PASS, 决策 #18 4 层测试金字塔达成)
- **L4 E2E 测试**: 1 + 1 + 1 + 0 + 0 + 0 + 0 + 0 + 1 + 1 + 1 = **6 项** (匹配 TEST_STRATEGY v1.1 §11 P0 6 条关键路径)
- **Visual baseline**: Landing + /admin/login + /onboarding/step-1 + /brand/overview + /brand/competitors + /industry/overview = **6 张** (匹配 MVP 关键路径覆盖, Frank 决策 2026-04-26; 完整 40 张留 Phase 2)
- **补救测试**: Session 1'/1.5'/1.2'/2'/2.1'/3'/4b' 七个 Python 重写 Session 必带 master TS 测试翻译段, 不丢失 master 727 vitest 已覆盖路径

### 5.3 Hard Fail 卡控规范

每个 Session 的 Phase Gate `§4` 必须满足以下 6 条之一才允许 PR merge:
1. **L1 Harness selftest**: `python scripts/ci-harness-selftest.py` 全绿 (规则数 + EXPECTED_POSITIVES 一致)
2. **L2 pytest**: 覆盖率 ≥ 80% (`pyproject.toml` 阈值)
3. **L3 集成测试**: 本 Session 矩阵列出的 L3 项目**全部跑绿** (跑不绿 = block merge)
4. **L4 E2E**: 本 Session 矩阵列出的 L4 项目**全部跑绿** (Playwright Python, preview env)
5. **Visual baseline**: 本 Session 矩阵列出的 baseline png **全部建立 + 后续 PR 必须不破** (Playwright `toHaveScreenshot()` diff < 0.1%)
6. **补救测试** (Python 重写 Session 才有): master TS 测试翻译数 ≥ 矩阵列示数, 单条都不能少

**Soft warning 拒绝**: Frank 决策 "跑不绿不让 commit", CI workflow 必须 hard fail. GitHub Actions branch protection rule 把 `ci.yml` job 设为 `required check`.

### 5.4 不在本矩阵的延后项

- **40 张完整 Visual baseline**: 矩阵只覆盖 6 张 MVP 关键路径, 完整 40 张 (TEST_STRATEGY v1.1 §11) 留给 Phase 2
- **OpenAPI 契约自动生成**: Session 0' Phase Gate 含 `/openapi.json` 暴露, 但 frontend `openapi-typescript` client 自动生成留 Session 4b'
- **CI 自修复 (Phase 4)**: TEST_STRATEGY v1.1 §11.4 列的"AI 自动修复 broken test" 不在 MVP 范围, 11 Session 全程不实施

---

## 6. Milestone 分组 (4 Milestone × 2-3 周 = 8-10 周)

| Milestone | Sessions | 时长 | 端到端可验证产物 (Milestone 末必交付) |
|---|---|---|---|
| **M1 · Auth 端到端 + Preview 基线** | Session 0' + A0' + 4a' (Auth UI 部分) | 2-3 周 | Frank 在 preview URL 注册新账号 → 收 verify email → 登录 → 看到空 Dashboard; Admin 用 super_admin 邮箱登录 preview Admin |
| **M2 · Pipeline 端到端 + Admin Response 可见** | Session 1' + 1.5' + 1.2' | 2-3 周 | Frank 在 preview 触发 1 条 ChatGPT/Doubao/DeepSeek query, Admin 看到 3 行 ai_responses; 鲁班自动注册 doubao 账号入池 |
| **M3 · 用户态报表 + MCP API** | Session 2' + 2.1' + 3' | 2-3 周 | Frank 在 preview /brand/overview?brandId=loreal 看到真实数据; MCP API curl 返回 JSON; CSV 导出可用 |
| **M4 · IA v2.0 完整 + Admin Beta** | Session A1' + 4b' | 2-3 周 | Frank 完成端到端 user journey (注册→onboarding→Brand/Industry Mode 切换→CSV→MCP); Playwright E2E 6 路径全绿; Admin Beta 上线 |

**总计**: 11 Session, 4 Milestone, 8-10 周, 每 Milestone 末 1 个 preview deploy + 端到端可验证产物。

---

## 7. 横切要求 (所有 Milestone 共用)

### 7.1 CI/CD 流水线

- **GitHub Actions 三 workflow**:
  - `ci.yml` (PR-on, 必过): ruff + mypy strict + pytest + eslint + vitest + build
  - `preview.yml` (PR-on, 必过): Render preview API + Vercel preview frontend + Supabase preview project + 评论 PR 带 preview URL
  - `deploy.yml` (main-on): prod 部署 (M4 后启用, M1-M3 暂不需要 prod)

### 7.2 环境变量管理

- **三套环境**: preview / staging (M3 后) / prod (M4 后)
- **Secret 来源**: GitHub Secrets + Vercel Environment Variables + Render env vars (三套独立)
- **关键 secret**: `VOLC_API_KEY` / `LUBAN_API_KEY` / `RESEND_API_KEY` / `DATABASE_URL` / `JWT_SECRET` / `REDIS_URL` / `STRIPE_KEY` (Phase 2)

### 7.3 数据库

- **preview env 独立 PostgreSQL**: Supabase free tier (`project_genpano_preview`)
- **staging/prod 独立 PostgreSQL**: M3+ 启用
- **Alembic auto-migration**: 每次 preview deploy 自动跑 `alembic upgrade head`

### 7.4 Worker (Celery)

- preview env Worker 跑 docker-compose 的 6 队列 (llm_chatgpt / llm_doubao / llm_deepseek / analysis / account_login / beat)
- 每个 Milestone 末 Worker 必须能跑通至少 1 条真实 task (M2 起强制)

### 7.5 Harness 迁移计划 (TS 38 grep → Python)

| master Group | Python 实现 | 备注 |
|---|---|---|
| **Group A (i18n/文案)** | A1-A6 仍扫 frontend .tsx (eslint plugin) | 前端规则不受 Python 化影响 |
| **Group B (图表契约)** | B1-B7 仍扫 frontend .tsx | 同上 |
| **Group C (V2 分析页)** | C9-C15 仍扫 frontend .tsx | 同上 |
| **Group D (产品决策)** | D1-D10 部分 Python (D8/D9/D10 Auth) + 部分 frontend | 双侧 |
| **Group E (Citation+KG)** | E1-E4 用 Python ruff/AST 扫 backend/app/ | 后端规则全 Python |
| **Group F (Adapter)** | F1-F4 用 Python ruff 扫 backend/app/engines/ | 后端规则全 Python |
| **Group G (Planner)** | G1-G4 用 Python ruff 扫 backend/app/platform/planner/ | 后端规则全 Python |
| **Group H (LLM Refinement)** | H1-H3 用 Python ruff 扫 backend/app/platform/planner/ | 后端规则全 Python |

### 7.6 测试迁移计划

| master | Python 实现 |
|---|---|
| **727 vitest 单测 (backend/)** | pytest + pytest-asyncio + httpx.AsyncClient (≥ 80% 覆盖) |
| **vitest UI 单测 (frontend/)** | vitest 不变 (frontend 是 React+Vite) |
| **HAR replay 集成测试** | pytest-playwright + routeFromHAR |
| **Playwright E2E** | Playwright Python (M4) |
| **Visual regression** | Playwright `expect(page).toHaveScreenshot()` (Python) |
| **OpenAPI 契约** | FastAPI 自动生成 `/openapi.json` + 前端 `openapi-typescript` 生成 client |

---

## 8. 不变项 (战略层)

> 以下是无论后端栈是 TS 还是 Python 都不变的内容, 决策 #1-#28 的精华全部保留:

- **MVP 引擎**: ChatGPT + Doubao + DeepSeek-CN
- **MVP 行业**: 美妆个护 / 奢侈品 / 食品饮料 / 服装时尚
- **商业模式**: 工具免费 + 行业 GEO 数据报告 + 优化咨询
- **Auth-Required Data Viewing** (决策 #9)
- **Onboarding 4 步 + 草稿 72h** (决策 #10/#21)
- **Citation 5 Tier 权重 1.0/0.7/0.4/0.15** (决策 #19)
- **提及率 non-brand 口径 (dimension='品类')** (决策 #16)
- **三视角 Report 框架 (operator/manager/branding)** (决策 #15)
- **Brand/Industry Mode IA v2.0** (决策 #20)
- **Citation 6 行动面 (归因诊断 / 内容策略 / 外联 PR / 竞品解构 / Simulator / MCP)** (决策 #19)
- **MCP Server 3 工具 (Auth-Required Bearer token)** (决策 #9)
- **行业知识图谱: Industry → Category → Brand → Product + 关系边**
- **Session Prompt 7+5 公约** (决策 #25)

---

## 9. 风险与应对

| 风险 | 应对 |
|---|---|
| **Python 重写工期估算偏低** (ts → py 不是 1:1 翻译, Python async 生态学习成本) | M1 完成后回看实际节奏, 必要时把 11 Session 降到 9-10 Session (合并 Session 2'+2.1') |
| **Camoufox 在 preview env (Render) 不稳定** | M2 末若 preview Camoufox 跑不起来, 降级为 mock_proxy + 真实 ai_responses 用 cached_replay 兜底, 不阻断 Milestone gate |
| **鲁班 SMS 在 preview env 跑不通** (临时号资源限制) | M2 末若 preview Luban 跑不起来, ChatGPT/Doubao/DeepSeek 三家全走 CLI 手工 inject; M3 末再补真实自注册 |
| **Vercel preview 配额超限** (单仓 100 deploy/day) | 启用 Vercel Pro ($20/月), Frank 接受这个成本 |
| **Supabase preview free tier 慢/容量不足** | M3 末迁到 Neon free tier (10 GB 容量) 或 Supabase Pro |

---

## 10. 立即可执行的下一步

1. **本文档评审**: Frank 确认 11 Session + 4 Milestone 划分, 确认 8-10 周时间预期 — 在 CLAUDE.md 加决策 #28' (本文件即决策依据)
2. **`docs/PRODUCT_PLAN.md` 同步**: 更新 MVP 时间线为 8-10 周 (原 4 周作废)
3. **Session 0' Prompt 起草**: 按公约 1-12 (决策 #25 Phase 2) 起草 Prompt, 含 §1 真相源索引 + §2 MVP scope 双列表 + §4 STOP triggers + §5 16 步交付顺序 + §6 Phase Gate
4. **CLAUDE.md 决策 #28' / #28'' 加载入文件头**: 把 Python pivot + preview env 横切要求加为 CLAUDE.md 决策项 (本文件作引用源)
5. **master Session 后端 TS 代码归档**: `git checkout -b archive/master-typescript-backend-2026-04-26`, 然后主分支删除 backend/src/** TS 后端代码 (frontend/ 不动, docs/ 不动), Frank 决定何时执行 (建议 M1 启动前)

---

## 11. 引用真相源

- `CLAUDE.md` 决策 #1-#28 (战略层全部生效)
- `docs/PRD.md` 425KB master PRD (战略层不变)
- `docs/ADAPTER_CONTRACT.md` (Adapter 契约不变, 实现栈 = Python)
- `docs/DATA_MODEL.md` (Schema 语义不变, 实现 = SQLAlchemy + Alembic)
- `docs/TEST_STRATEGY.md` v1.1 (测试 4 层 + 4 支柱不变, runner = pytest + Playwright)
- `docs/REVIEW_2026_04_21.md` (P0 8 gap 闭环不变)
- `docs/HARNESS_ENGINEERING.md` (方法论不变, 实现 = ruff + mypy + AST visitor)
- 合并仓 `geo_tracker/` (Python 实战代码, ~10000 行, Session 1'/1.5'/1.2' 直接接纳为实施层基线)
- 合并仓 `frontend/src/` (Auth 8 页面 TSX, Session 0'/4a' 直接接纳为前端实施层基线)
- **不引用**: 合并仓 `PRD.md` / `docs/PRD-auth.md` / `docs/scraping-experience-report.md` / `docs/plan-profile-aware-execution.md` / `docs/test-plan-auth.md` 等所有合并仓 .md 文档 (Frank 2026-04-26 明确指示版本不对)

---

**文档版本**: v1.0 (2026-04-26)
**Next**: 本文件评审通过后, 立刻起草 Session 0' Prompt
