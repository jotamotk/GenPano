name: Deliverable Task
description: Track one fast-path or full-path deliverable with a clear execution contract
title: "[Task] "
labels:
  - status:ready
body:
  - type: markdown
    attributes:
      value: |
        Use this template for one deliverable, not one agent role. A task should normally map to one branch and one PR.
  - type: dropdown
    id: path
    attributes:
      label: Path
      description: Select the workflow path for this deliverable.
      options:
        - fast
        - full
    validations:
      required: true
  - type: dropdown
    id: owner-hat
    attributes:
      label: Owner Hat
      description: Select the responsibility hat currently owning execution.
      options:
        - lead
        - frontend-visualization
        - frontend-integration
        - backend-api
        - pipeline-data
        - qa-e2e
        - release-ci
        - review
        - pruning
    validations:
      required: true
  - type: input
    id: coordination-issue
    attributes:
      label: Coordination Issue
      description: Link the Epic/coordination issue if this is Full Path. Use "none" for standalone Fast Path work.
      placeholder: "#120 or none"
    validations:
      required: true
  - type: textarea
    id: goal
    attributes:
      label: Goal
      description: State the deliverable outcome in one or two sentences.
      placeholder: "Fix the Admin retry error classification for..."
    validations:
      required: true
  - type: textarea
    id: current-state
    attributes:
      label: Current State
      description: State what is true now. Keep this current as status changes.
      value: |
        - Path:
        - Current judgment:
        - Branch:
        - PR:
        - Blocker:
        - Next:
    validations:
      required: true
  - type: textarea
    id: decisions
    attributes:
      label: Decisions
      description: Record settled decisions only. Copy DECISION comments here when they change execution.
      value: |
        - Pending.
    validations:
      required: true
  - type: textarea
    id: execution-contract
    attributes:
      label: Execution Contract
      description: Inline the contract. Do not rely on unresolved "depends on #..." pointers as the only source of scope.
      value: |
        - Branch:
        - Allowed Scope:
          -
        - Forbidden Scope:
          -
        - Contract Snapshot:
          -
        - Dependencies:
          -
    validations:
      required: true
  - type: textarea
    id: prd-source
    attributes:
      label: PRD Source
      description: Required for product behavior. Use "none - fast path bugfix" only when no PRD or product contract changes.
      value: |
        - PRD:
        - Requirement IDs:
          - PRD-AREA-FEATURE-001
        - PRD status: approved | PRD-CHANGE needed | none
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance Criteria
      description: List the exact checks that make this deliverable complete.
      value: |
        - [ ] Define the acceptance check.
    validations:
      required: true
  - type: textarea
    id: verification
    attributes:
      label: Verification
      description: Required local tests, CI checks, screenshots, Playwright traces, or live checks.
      value: |
        - [ ] Local:
        - [ ] Screenshot/video:
        - [ ] Playwright:
        - [ ] CI/CD:
        - [ ] Live `http://116.62.36.173/` check if production-facing:
    validations:
      required: true
  - type: textarea
    id: context-links
    attributes:
      label: Context Links
      description: Link docs, code paths, screenshots, traces, failing requests, prior issues, or prior PRs.
      placeholder: "- docs/...\n- backend/static/admin.html\n- PR #..."
    validations:
      required: false
  - type: textarea
    id: handoff
    attributes:
      label: Handoff
      description: What should this deliverable pass to review, QA, release, or the next task?
      placeholder: "Return changed routes, test command output, evidence links, and known risks."
    validations:
      required: true
  - type: textarea
    id: closure
    attributes:
      label: Closure
      description: Complete this before closing. Use Completed, Won't Do, Split/Superseded, or Duplicate.
      value: |
        - Type:
        - Reason:
        - Linked PR/commit or replacement/canonical issue:
        - Verification evidence:
    validations:
      required: false
