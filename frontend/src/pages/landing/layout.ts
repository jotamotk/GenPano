/**
 * Shared layout constants for the landing page.
 *
 * Moved from LandingPage.tsx (line 419). Used by every section to share the
 * same 1200px-wide centered column.
 */
export const MAX_W = 'max-w-[1200px] mx-auto px-6';
