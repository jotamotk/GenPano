# 核心
fastapi>=0.111
uvicorn[standard]>=0.29
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13

# 任务队列
celery[redis]>=5.3
redis>=5.0

# 浏览器 & 反指纹
camoufox>=0.4          # 反指纹浏览器，手动设置 locale，避免构建期下载 GeoIP
playwright>=1.58

# 验证码
httpx>=0.27            # CapSolver HTTP 客户端
openai>=1.0            # 火山引擎 Ark API（OpenAI 兼容，视觉验证码求解）
Pillow>=10.0           # 滑块验证码缺口检测（本地图像分析）

# 工具
python-dotenv>=1.0
pydantic-settings>=2.0
json-repair>=0.25       # 容错解析 LLM 返回的脏 JSON

# 测试
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20          # SQLite async driver（测试用）
anthropic>=0.25          # Claude SDK
